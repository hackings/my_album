<br />

<%= form_tag albums_path, :method => :get do %>
  Search - <%= text_field_tag :search, params[:search] %>
  <%= submit_tag('Search')%>
<% end %>

<table>
 <tr>
  <td>List of albums</td>
  <td></td>
  <td></td>
 </tr>

<% @albums.each do |album| %>
  <tr>
    <td><%= album.name %></td>
    <td><%= link_to album %></td>
    <td></td>
  </tr>
<% end %>

</table>

<%= link_to 'New Album', new_album_path %>
